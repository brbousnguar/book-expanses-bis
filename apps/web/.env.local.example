# Copy to .env.local and adjust.
# For local dev with the in-memory backend (no AWS, no Cognito):

NEXT_PUBLIC_API_URL=http://localhost:3001

# Leave these unset for local dev (no login; backend uses a fake user):
# NEXT_PUBLIC_COGNITO_USER_POOL_ID=
# NEXT_PUBLIC_COGNITO_CLIENT_ID=

# For deployed API + Cognito, set all three:
# NEXT_PUBLIC_API_URL=https://xxx.execute-api.region.amazonaws.com
# NEXT_PUBLIC_COGNITO_USER_POOL_ID=...
# NEXT_PUBLIC_COGNITO_CLIENT_ID=...
